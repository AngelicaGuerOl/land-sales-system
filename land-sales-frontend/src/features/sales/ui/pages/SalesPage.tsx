import AddRoundedIcon from '@mui/icons-material/AddRounded'
import VisibilityRoundedIcon from '@mui/icons-material/VisibilityRounded'
import { Alert, Box, Button, Chip, FormControl, InputLabel, MenuItem, Paper, Select, Stack, Table, TableBody, TableCell, TableHead, TablePagination, TableRow, TextField, Typography } from '@mui/material'
import { useEffect, useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { EmptyState } from '../../../../shared/ui/components/EmptyState'
import { LoadingScreen } from '../../../../shared/ui/components/LoadingScreen'
import { PageContainer } from '../../../../shared/ui/layout/PageContainer'
import { formatCurrency } from '../../../../shared/utils/formatters'
import { useSales } from '../hooks/useSales'

const dateLabel = (value: string) => new Intl.DateTimeFormat('es-MX', { dateStyle: 'medium' }).format(new Date(`${value}T00:00:00`))
export function SalesPage() {
  const navigate = useNavigate(); const [searchInput, setSearchInput] = useState(''); const [search, setSearch] = useState(''); const [status, setStatus] = useState(''); const [dateFrom, setDateFrom] = useState(''); const [dateTo, setDateTo] = useState(''); const [page, setPage] = useState(0); const [size, setSize] = useState(25)
  useEffect(() => { const timer = window.setTimeout(() => { setSearch(searchInput.trim()); setPage(0) }, 350); return () => window.clearTimeout(timer) }, [searchInput])
  const query = useSales({ page, size, search, status, dateFrom, dateTo }); const rows = query.data?.content ?? []
  return <PageContainer><Stack spacing={2}><Stack direction={{ xs: 'column', sm: 'row' }} sx={{ justifyContent: 'space-between', alignItems: { sm: 'center' } }}><Box><Typography variant="h4" sx={{ fontWeight: 700 }}>Historial de ventas</Typography><Typography color="text.secondary">Consulta las ventas de lotes registradas.</Typography></Box><Button variant="contained" startIcon={<AddRoundedIcon />} onClick={() => navigate('/ventas/nueva')}>Nueva venta</Button></Stack><Stack direction={{ xs: 'column', lg: 'row' }} spacing={1.5}><TextField fullWidth label="Buscar por folio, cliente, teléfono o lote" value={searchInput} onChange={(event) => setSearchInput(event.target.value)} /><FormControl sx={{ minWidth: 160 }}><InputLabel id="sale-status">Estado</InputLabel><Select labelId="sale-status" label="Estado" value={status} onChange={(event) => { setStatus(event.target.value); setPage(0) }}><MenuItem value="">Todos</MenuItem><MenuItem value="ACTIVE">Activas</MenuItem></Select></FormControl><TextField type="date" label="Desde" value={dateFrom} onChange={(event) => { setDateFrom(event.target.value); setPage(0) }} slotProps={{ inputLabel: { shrink: true } }} /><TextField type="date" label="Hasta" value={dateTo} onChange={(event) => { setDateTo(event.target.value); setPage(0) }} slotProps={{ inputLabel: { shrink: true } }} /></Stack>{query.isLoading ? <LoadingScreen message="Cargando ventas..." /> : null}{query.isError ? <Alert severity="error">No fue posible cargar las ventas.</Alert> : null}{!query.isLoading && !query.isError && rows.length === 0 ? <EmptyState title="Sin ventas registradas" description="Registra una nueva venta para comenzar." /> : null}{rows.length > 0 ? <Paper sx={{ overflowX: 'auto' }}><Table size="small"><TableHead><TableRow>{['Folio', 'Fecha', 'Cliente', 'Lotes', 'Total', 'Enganche', 'Financiado', 'Estado', 'Acción'].map((heading) => <TableCell key={heading} sx={{ fontWeight: 700, whiteSpace: 'nowrap' }}>{heading}</TableCell>)}</TableRow></TableHead><TableBody>{rows.map((sale) => <TableRow key={sale.id} hover><TableCell sx={{ fontWeight: 700 }}>{sale.folio}</TableCell><TableCell>{dateLabel(sale.saleDate)}</TableCell><TableCell>{sale.customerName}<Typography variant="caption" sx={{ display: 'block' }} color="text.secondary">{sale.customerPhone}</Typography></TableCell><TableCell>{sale.lotCodes.join(', ')}</TableCell><TableCell>{formatCurrency(sale.totalAgreedPrice)}</TableCell><TableCell>{formatCurrency(sale.totalDownPayment)}</TableCell><TableCell>{formatCurrency(sale.totalFinancedAmount)}</TableCell><TableCell><Chip label="Activa" color="success" size="small" /></TableCell><TableCell><Button size="small" startIcon={<VisibilityRoundedIcon />} onClick={() => navigate(`/ventas/${sale.id}`)}>Ver detalle</Button></TableCell></TableRow>)}</TableBody></Table></Paper> : null}<TablePagination component="div" count={query.data?.totalElements ?? 0} page={page} rowsPerPage={size} onPageChange={(_, value) => setPage(value)} onRowsPerPageChange={(event) => { setSize(Number(event.target.value)); setPage(0) }} rowsPerPageOptions={[10, 25, 50]} labelRowsPerPage="Filas por página" labelDisplayedRows={({ from, to, count }) => `${from}-${to} de ${count}`} /></Stack></PageContainer>
}
